{
  "name": "openbmc-tests",
  "version": "1.0.0",
  "description": "Selenium tests for OpenBMC WEB interface",
  "scripts": {
    "test": "mocha main.js --reporter mocha-junit-reporter --reporter-options mochaFile=../../reports/selenium-junit.xml"
  },
  "dependencies": {
    "axios": "^1.12.2",
    "chromedriver": "^142.0.0",
    "selenium-webdriver": "^4.35.0"
  },
  "devDependencies": {
    "mocha": "^11.7.5",
    "mocha-junit-reporter": "^2.2.1"
  }
}
